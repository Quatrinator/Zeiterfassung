services:
  nginx:
    ports: !override
      - "127.0.0.1:18080:8080"
  php:
    environment:
      APP_URL: http://localhost:18080
      APP_ENV: testing
      COOKIE_SECURE: "0"
      TEST_MODE: "1"
    volumes:
      - ./tests:/var/www/app/tests:ro
